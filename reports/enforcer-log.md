# Enforcer log

## 2026-07-21 — frontend-developer mobile-responsive fan-out (T121-T127)

Session `cc4f55ca-bce5-4f8f-b0ae-9e1405c0f415` dispatched 6 background `frontend-developer` agents for T121-T127 (mobile-responsive redesign) and ended before any of their results were collected (`ScheduleWakeup` call failed on a missing `prompt` param, then the session just stopped). Checked against actual repo state (`git diff`):

- **T127 (modals/panels/forms) never happened.** Assigned files: `CardDetailModal.tsx`, `LabelsPanel.tsx`, `MembersPanel.tsx`, `BackgroundPanel.tsx`, `IntegrationsPanel.tsx`, `DashboardPage.tsx`, `CalendarPage.tsx`, `DocumentationPage.tsx` — zero diff on any of them. `LoginPage.tsx`/`SignupPage.tsx` only carry the earlier T114 rebrand diff, no mobile touch.
- **T122's actual fix (Kanban list-column width) is unmet.** The orchestrator scoped that agent to "Edit only `src/pages/BoardPage.tsx`", but the hardcoded `w-72` list-column width that makes the Kanban view too wide for phone lives in `src/components/ListColumn.tsx` (confirmed untouched, `w-72` still unconditional, no `sm:` variant). The agent could only cosmetically adjust the header/nav/add-list-form inside BoardPage.tsx — the thing T122 actually asked to be fixed was made structurally impossible by the file-scope restriction.
- TASKS.md was never updated: T121-T128 still read as their original un-annotated task descriptions, no "Done" writeups exist for any of them.
- T119/T128 (live/browser end-to-end verification) never ran.

Not (yet) logging this as a proposal — first occurrence seen for this specialist/pattern in this project's memory. Worth checking again if a future BoardPage/ListColumn split-scope task shows the same gap.

**Resolution (same day, same session, later that turn):** this log was written from a snapshot taken mid-flight, before the 6 background agents' completion notifications had actually arrived — the session was still running and picked back up, not abandoned. All three gaps above were closed in the same session: T127 landed (8 files, confirmed via `git diff`), the `ListColumn.tsx` scope gap was caught independently by both the orchestrator and the T121/T122 agent itself (which correctly flagged it as a blocker rather than editing outside its given scope) and fixed directly (`w-72` → `w-[85vw] max-w-xs sm:w-72` at both call sites), and TASKS.md/goal.md were updated with a full writeup after a centralized `npm run build`/`lint`/`tests/e2e/` pass (8/8) plus a live phone-viewport verification script. The one real pattern worth keeping for next time: splitting a single component's responsive fix across a file-scoped agent boundary (BoardPage.tsx vs. ListColumn.tsx, the same "board" concept split across two files) is a real risk — an agent scoped to one file can't fix a bug whose actual code lives in a sibling file, and will (correctly) report a blocker instead of silently expanding scope. Worth checking file boundaries against a quick grep for the target CSS class/prop *before* writing a single-file dispatch prompt, not just trusting the task description's line-number references.

## 2026-07-22 — T142/T143 modal-height write-up overstates the ratio target (needs another pass)

Session `5b249e53-c501-455c-8c44-a1fea996306a` (interactive, working goal.md's T139/T140b + T142/T143). The modal-height implementation itself is real and the two hard numeric bars are genuinely met: height went from a self-reported ~522-531px to a live-measured 840px (+~310px, matching the "150-300px taller" ask closely enough), and the bottom gap is exactly 20px (`getBoundingClientRect()`, not eyeballed). Both `npm run build`/`npm run lint` clean, confirmed from real tool output, not just claimed.

The problem is the third criterion in goal.md's own "Done when" bullet: "**roughly a 4:7 width:height ratio**." The specialist's own `verify-t142-t143.mjs` measured the modal's live box at 840×840 and printed `width:height ratio ≈ 1.000 (4:7 ≈ 0.571)` in its own output — i.e. its own check shows the box is square, not the requested ~4:7 (taller-than-wide) shape, because the new CSS ties height to `100vh` and width to `60vw` independently, so the actual ratio just falls out of whatever the test viewport's own aspect ratio happens to be (it was 1400×900). Neither the TASKS.md T142/T143 Done entry nor goal.md's Notes ever states this measured 1.000 number — both instead quote only the "adding on the order of 150-300px" half of the goal sentence and call the result "consistent with 'roughly 4:7... on the order of 150-300px'", which reads as the ratio target being met when the specialist's own tool call shows otherwise.

Not filing a proposal yet — first time this specific pattern (a self-run check contradicts the target, and the write-up quotes only the half of the spec that still matches) has shown up in this project's enforcer log. Worth escalating to a proposal if a future pass shows the same selective-quoting move again.

## 2026-07-22 — T144/T145/T146 (parallel frontend-developer worktrees): T146's own verify script broken by T144's merge (needs another pass)

Two `frontend-developer` agents ran in parallel worktrees (T144+T145 in one, T146 in the other), later merged into one working tree. Re-ran everything independently rather than trusting the reports:

- **T144 CONFIRMED, T145 CONFIRMED.** `git diff` matches the claimed toggle-state/SVG/pre-flight-check changes exactly, both toggle `<button>`s are `type="button"` (checked statically and behaviorally — clicking the toggle never submits/navigates on either page, confirmed with a throwaway script). Reran `verify-t144-t145.mjs` live: all checks pass, matching the claim.
- **T146's underlying fix is CONFIRMED real** (`AuthContext.tsx`'s render-time-stabilized `user` state, `CalendarPage.tsx`'s focus-listener removal, `BoardPage.tsx` genuinely untouched — all match the diff claim) and the full `tests/e2e/` suite passes 8/8 clean on a single run.
- **But the specific claim "`verify-t146.mjs` ... 5 scenarios total, all passing" is false as committed.** Running it against the current merged code fails immediately at the sign-up step: `getByLabel('Contraseña')` (line 84, no `exact: true`) is a strict-mode violation now that T144's toggle button carries an aria-label containing the substring "contraseña" — the exact ambiguity the T144 agent already fixed at 3 other call sites (`tests/e2e/fixtures.ts` x2, `tests/e2e/auth.spec.ts` x1), just never in this sibling worktree's own verify script, since the two agents never saw each other's changes until the orchestrator's merge. Confirmed deterministic on a second run. Patched a throwaway copy with the same one-line `{ exact: true }` fix used elsewhere and reran it: all 5 scenarios then pass, confirming this is a stale/broken verification artifact, not a broken feature — but the artifact as delivered does not run, contradicting the "all passing" claim.

Not filing a proposal yet — first occurrence of this exact pattern (a sibling parallel-worktree agent's own verify script goes stale from another agent's UI change, post-merge) in this project's enforcer log, and the existing standing instruction (fan out `isolation: worktree` for file-independent parallel work, one centralized `tests/e2e/` pass after merge) already covers the *shared* test suite — it just doesn't cover each agent's own throwaway `verify-*.mjs` script, which nobody but the enforcer re-runs post-merge. Worth a proposal if this shows up again: something like "before reporting a `verify-*.mjs` script as passing, grep it for any accessible-name/`getByLabel` selector that a sibling agent's own aria-label additions could now collide with, post-merge" — but one instance isn't yet a pattern.
