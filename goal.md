# Current goal

**Set:** 2026-07-14
**Status:** done

Confirmed via a HelmControl New Goal intake chat ("Start This Goal"): build out a one-to-one Trello clone on Supabase — multi-user accounts with permissions layered on Supabase auth, board owners inviting others by username or email, cards attaching files of any type (images, scripts, zip archives, etc.) or links with image attachments getting a visual preview/thumbnail rather than just a filename, and fully customizable board backgrounds (color wheel or uploaded photo, matching the full range of options in the referenced screenshot). Three pieces of that restated scope are genuinely new work not yet built: cards must show a "created at" timestamp, the app needs an in-app-only notification section (no email) that surfaces invites and other relevant events, and image attachments need a visual thumbnail preview instead of a plain filename link.

## Done when
Every card in the UI displays its creation timestamp. A notifications section (in-app only, no email delivery) exists, surfaces at least board-invite events plus other relevant events (exact event set to be scoped during implementation — e.g. member added/removed, invite received), is visible to the relevant user, and updates/persists correctly across a page reload. Image-type card attachments render a visual thumbnail preview (not just the filename), while non-image attachments keep the current filename/link treatment. All three work end-to-end against the live Supabase backend (project ref `rgzufvoobohfmewylqji`), not just in isolated unit tests. The rest of the restated scope (permissioned multi-user accounts, username/email invites, any-file-type card attachments plus links, customizable board backgrounds) is already shipped and verified — see Notes.

## Notes
- Origin: confirmed via HelmControl New Goal intake chat on 2026-07-14 ("Start This Goal"). That chat restated the full product scope; cross-checking against TASKS.md and the actual component code shows everything except the three "Done when" items above is already built and verified live.
- Correction made during this session: an earlier pass at this same goal.md claimed card attachments were fully shipped and verified (T033/T036), but the confirmed summary's "image attachments get a visual preview/thumbnail" detail was never actually implemented — verified by reading CardDetailModal.tsx (attachments render as plain filename links/anchors, no `<img>`) and CardItem.tsx (no thumbnail on the card face either). Added as a third new-work item above and flagged for organizer to task out.
- Already shipped and verified end-to-end against the live backend (see TASKS.md): base clone — Supabase Auth, board/list/card CRUD, drag-and-drop, labels, checklists, comments, owner/member roles via RLS (T001-T031); board invites by username or email (T026, T032, T035); card attachments of any file type plus link attachments, minus the thumbnail preview noted above (T033, T036); customizable board backgrounds via color wheel or uploaded photo (T034, T037).
- Still open from the prior goal, unrelated to this one's new asks: the checked-in Playwright suite (tests/e2e/) can't run until `npm install` is approved to add `@playwright/test` to node_modules (see TASKS.md Pending, T028/T029/T030 automated suite) — everything it checks has been verified manually instead.
- Permissions preview shown to the user before confirming this goal: dependency installs or upgrades (npm/pip/cargo/brew/yarn/pnpm) — plausibly relevant here if notifications need a new realtime/toast library, though Supabase Realtime (already a dependency) may cover it without one.
- This session's job was limited to writing this goal.md and delegating to organizer for a TASKS.md breakdown of the new items — no implementation started yet. Note: the working tree also has uncommitted, unstaged changes to CardItem.tsx/BoardPage.tsx/DashboardPage.tsx/types/index.ts plus new NotificationsBell.tsx/NotificationsPanel.tsx and two notification migrations — these appear to be in-progress implementation from a separate session, not from this goal-writing run.
- 2026-07-14 (later): All three "Done when" items now built and verified live against the same Supabase project. Card timestamps (T038/T039) and the notifications system (T040-T043, table + RLS + two triggers + panel/bell UI) verified in the same pass as the prior note above. Image-attachment thumbnails (T044/T045) verified separately afterward: uploaded a real PNG and confirmed a visual `<img>` thumbnail renders (not just a filename), uploaded a .txt and confirmed it correctly stays a plain link, reloaded the page and confirmed the thumbnail persists via a freshly-generated signed URL each load. All verification was live browser automation plus direct `supabase db query` against the RLS policies, not unit tests.
