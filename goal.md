# Current goal

**Set:** 2026-07-22 (new goal, confirmed via a seventh HelmControl New Goal intake — supersedes the modal-height/calendar-feed goal below. That goal is NOT complete (calendar-feed redeploy still needs the user to run one command; the modal's width:height ratio bullet was explicitly declined rather than fixed) — its history is carried forward unchanged, not dropped, and remains open.)
**Status:** done (2026-07-22) — all three "Done when" items implemented and verified live (T144-T146 in TASKS.md's Done section): password show/hide toggles on both signup and login pages (independent per-page state), password strength validation rejecting all-letters/all-numbers passwords and accepting mixed on signup, and tab refocus/reopen no longer triggers full data refetch (root cause stabilized `user` reference in AuthContext, removed unrelated CalendarPage focus listener).

Three fixes to the TAMS app: (1) add a show/hide toggle on password fields during account creation, (2) enforce a password-strength rule on signup — reject passwords that are only letters or only numbers, requiring a mix of both, and (3) stop the app from doing a full reload/refetch every time it's opened; instead only re-fetch/ping the server when the user actually reloads a page or navigates to a different page, to cut unnecessary load on the servers and make the experience feel less jarring.

## Done when
- The signup password field has a working show/hide toggle (and the login password field, if in scope per the task breakdown) — verified live in a browser, not just by reading the code.
- Signup rejects a password that is all letters or all numbers with a clear error, and accepts one that mixes both — verified live against the real Supabase signup flow.
- Reopening/refocusing the app (e.g. switching back to its browser tab) no longer triggers a full data refetch; a refetch still happens on an explicit page reload or in-app navigation to a different page — verified live, not just by reading the code.

## Notes
- 2026-07-22: goal confirmed via a HelmControl New Goal intake ("Start This Goal"). Per this run's explicit scope limit, no implementation was started — handed off to organizer immediately after writing this to break the three fixes into concrete TASKS.md items.
- 2026-07-22, same day: T144-T146 fully implemented and verified live — see TASKS.md's consolidated Done entry for the full breakdown. Dispatched as 2 parallel frontend-developer agents in isolated worktrees (password toggle/validation work and refetch-stabilization work, independent file sets allowing true parallelism), merged by the orchestrator after both landed. `npm run build`/`lint` clean, `tests/e2e/` 8/8 passing clean in one run, live verification via two new `verify-*.mjs` scripts (both following this repo's established standalone `chromium.launch()` convention) confirmed: toggle flips and preserves typed value on both pages; client-side password validation rejects all-letters/all-numbers with exact error message and never calls Supabase endpoint for rejected attempts; mixed password succeeds; tab refocus does NOT trigger refetch, explicit reload and navigation DO. Throwaway test users/boards still live in database — same recurring cleanup blocker; exact identifiers need to be extracted from the two verify scripts before running cleanup command.
- Grounded by reading the current code directly before writing this (not guessed):
  - `src/pages/SignupPage.tsx`: password `<input>` is `type="password"` with `minLength={6}` and no strength check beyond length, and no show/hide toggle exists (fix 1 and 2 both apply here). `src/pages/LoginPage.tsx` also has a plain password field with no toggle.
  - Likely root cause for fix 3: `src/contexts/AuthContext.tsx`'s `useEffect` calls `supabase.auth.getSession()` once and otherwise relies on `supabase.auth.onAuthStateChange`, which supabase-js fires on its own internal `visibilitychange`-driven token-refresh checks (i.e. whenever the tab regains focus/is reopened), each time handing back a *new* `session` object even when nothing meaningful changed. `src/pages/BoardPage.tsx`'s main data-load `useEffect` (around line 154) depends on `[boardId, user]`, and `user` is derived as `session?.user ?? null` — a new object reference on every such auth-state event — so the board's full fetch effect refires on refocus/reopen, not just on an actual navigation or reload. `src/pages/CalendarPage.tsx` separately has its own explicit `window.addEventListener('focus', ...)` refetch (around line 163), which is a second, more direct source of the same symptom and likely also in scope.
  - Fix likely needs to distinguish "meaningfully new session" (different user/token requiring a refetch) from "same session, new object reference" (should not refetch) in `AuthContext`, and/or drop the effect dependency on `user` identity in favor of a stable id, plus reconsider `CalendarPage`'s focus listener. Left for organizer/implementer to scope precisely — this note is grounding, not a prescribed fix.
  - App is internally branded "TAMS" in the UI itself (see `SignupPage.tsx`'s logo mark), consistent with prior goals in this file — not a mismatch with this repo despite the directory being named "Trello".

---

# Previous goal (2026-07-22 — modal height + calendar-feed auth, in progress/blocked on user)

**Set:** 2026-07-22 (superseded same day by the TAMS three-fixes goal above, while still open — see that goal's Set line.)
**Status:** in progress — the modal-height fix (T142/T143) is implemented and live-verified for two of its three numeric bars (taller by ~310px, 20px bottom gap); the third (~4:7 width:height ratio) is not actually met — see the correction note below, kept open pending the user's call on whether a narrower modal is worth it. The calendar-feed auth fix's code (T094b) has been on disk since 2026-07-17; today's session confirmed via a direct `curl` against the live function that it was never actually redeployed (T139/T140b), attempted `supabase functions deploy calendar-feed` directly, and hit this environment's production-deploy guardrail — needs the user to run that one command themselves before this goal's first "Done when" item is met.

Two fixes to the TAMS app. First, the Google Calendar subscription feed link (the `calendar-feed` Supabase Edge Function URL with a `?token=` query param, surfaced on CalendarPage per T091) returns `{"code":"UNAUTHORIZED_NO_AUTH_HEADER", ...}` when Google Calendar tries to subscribe to it — external calendar clients don't send a bearer auth header, so the function's auth check needs to rely on the token query param instead of (or in addition to) a header, likely also requiring the function's JWT-verification setting to be adjusted. Second, the card detail modal (`CardDetailModal.tsx`) needs to be taller still — from its current ~4:3 (height:width) portrait ratio to roughly 4:7, i.e. about 150-300px more height at the bottom — while still leaving a 20px gap from the bottom of the screen rather than touching it.

## Done when
- Subscribing to the calendar feed URL from an actual external calendar client (e.g. Google Calendar's "From URL" subscribe) succeeds instead of returning `UNAUTHORIZED_NO_AUTH_HEADER` — verified live against the deployed Edge Function, not just by reading the repo's config file.
- The card detail modal is measurably taller than its current shape — roughly a 4:7 width:height ratio, adding on the order of 150-300px of height at the bottom — while still leaving a 20px gap from the bottom of the viewport rather than touching it.
- Both verified live in a real browser against the live Supabase backend.

## Notes
- 2026-07-22: goal confirmed via a sixth HelmControl New Goal intake ("Start This Goal"). Per this run's explicit scope limit, no implementation was started — handed off to organizer immediately after writing this to break the calendar-feed auth fix and the modal height increase into concrete TASKS.md items.
- Likely not a brand-new bug: TASKS.md's **T094b** already added `[functions.calendar-feed] verify_jwt = false` to `supabase/config.toml` (committed 2026-07-17, after T093's live verification first hit this exact `UNAUTHORIZED_NO_AUTH_HEADER` error), but T094b is still sitting in TASKS.md's Pending section, noted as needing one more `supabase functions deploy calendar-feed` from the user that was never confirmed to have happened. Confirmed by reading `supabase/config.toml` directly just now: the `verify_jwt = false` section for `calendar-feed` is still present on disk. So the most likely explanation is the fix was never actually redeployed live, not that it's wrong — worth checking the live deployed function's config before writing new code.
- Also confirmed by reading the code directly before writing this: the modal's current shape is `sm:aspect-[3/4] sm:max-h-[92vh] sm:w-[60vw]` in `CardDetailModal.tsx` (set in the most recent commit, eb53b00, "Make the card modal taller (3:4 portrait) instead of near-square") — already portrait, not square. This goal is a further height increase on top of that recent change, not a reversal of it.
- The prior goal below (board-scroll fix + card-modal redesign with collapsible fields/icons/animations) is complete per its own Status line; its history, plus the older Google OAuth blockers (T066/T077/T068/T069) carried inside it, are unchanged and not re-litigated here.
- 2026-07-22, same day: both TASKS.md items implemented in one pass. **Modal height (T142/T143): height and bottom-gap bars met; ratio target not.** `CardDetailModal.tsx`'s panel height changed from `sm:aspect-[3/4] sm:max-h-[92vh]` to an explicit `sm:h-[calc(100vh-3.75rem)]` — live-measured at a 1400×900 viewport: 840px tall (up from the prior shape's self-reported ~522-531px, a genuine ~310px increase, at the high end of the 150-300px ask), bottom edge exactly 20px above the viewport bottom (not flush), all 5 field buttons still usable, saved data survives close/reopen. **Correction (later same day, caught by an automated enforcer pass over this session's own work, not self-reported):** the "Done when" bullet's second half — "roughly a 4:7 width:height ratio" — was not actually met and the original write-up here overstated it. The live verify script's own measurement was `width:height ratio ≈ 1.000` (840×840, square), because width (`60vw`, capped at `max-w-4xl`) and height (`calc(100vh - 3.75rem)`) are independent formulas that only coincidentally landed on the same number at that test viewport. Hitting ~4:7 for real would mean tying width to ~57% of height (≈480px wide at a 900px-tall viewport) — a real, narrower-modal visual change, which the user was asked about (2026-07-22, later still) and declined for now in favor of just correcting the record; see TASKS.md's updated T142/T143 Done entry. This goal's second "Done when" bullet is therefore only partially met (taller + 20px gap: yes; ~4:7 shape: no) until/unless a narrower modal is explicitly requested. **Calendar-feed auth (T094b/T139/T140b): code confirmed correct, still not live.** Direct `curl -i` against the deployed function (with a token query param) still returns `401 UNAUTHORIZED_NO_AUTH_HEADER` at Supabase's platform gateway — the function's own token-checking code never runs. `supabase functions list` shows `calendar-feed` last deployed 2026-07-17, i.e. before T094b's `verify_jwt = false` fix was even committed to `supabase/config.toml` — so the fix was simply never redeployed, not wrong. Attempted `supabase functions deploy calendar-feed` directly (the CLI is authenticated and linked to this exact project); blocked by this environment's production-deploy guardrail, same recurring pattern as every other Edge Function deploy in this project's history (T063b/T075b/T094/T112). This goal's first "Done when" item is not met until the user runs that one command themselves — see TASKS.md's new **T140b** for the exact command.

---

# Previous goal (2026-07-21 — board-scroll fix + card-modal redesign, complete)

**Set:** 2026-07-21 (new goal, confirmed via a fifth HelmControl New Goal intake — supersedes the 2026-07-21 rebrand + mobile-responsive-redesign goal below, which is complete. That goal's own history is carried forward unchanged, not dropped.)
**Status:** done (2026-07-21) — all "Done when" items implemented and verified live (T129-T138 in TASKS.md's Done section): fixed-height board page with per-list internal scrolling, a ~58-59%-of-viewport rectangular card modal, all five fields converted to independent (non-accordion) collapsible icon buttons with custom Higgsfield-generated SVG icons in the app's indigo/violet palette, expand/collapse + entrance animations respecting `prefers-reduced-motion`, and a live verification pass confirming existing saved data (dates/location/labels/checklist) survives the collapse/expand/close/reopen cycle intact.

Two UI/UX reworks to the TAMS board. First, on the "Tableros" board page (`BoardPage.tsx`), the page itself currently grows taller as a list fills with cards — this needs to stop: each list column should scroll internally (its own vertical scrollbar) while the board page (header, view switcher, list row) stays a fixed height. Second, redesign the card detail modal ("Tarjeta", `CardDetailModal.tsx`) to be much larger — roughly 60% of the screen — while keeping its rectangular shape, and restructure its fields: Inicio (start date), Fin (due date), and Ubicación (location) become collapsible icon buttons like the existing Etiquetas pattern, revealing their content only on click; Etiquetas and Lista de verificación (checklist) get the same collapsible-button treatment. The new buttons need custom-designed icons, and animations should be added wherever they'd improve the feel — both using the Higgsfield/HuggingFace tooling already available for asset and animation generation.

## Done when
- On the board page, adding many cards to a list never grows the page's own height — each list column scrolls internally with its own vertical scrollbar, and the board page's outer layout (header, view switcher, list row container) stays a fixed height.
- The card detail modal is resized to roughly 60% of the screen while remaining rectangular (not circular/irregular), on both desktop and (given the just-completed mobile-responsive goal below) still usable at phone widths.
- Inicio, Fin, and Ubicación are each converted to a collapsible icon button (matching the existing Etiquetas interaction pattern) that reveals its field's content only when clicked, rather than being always-visible inline fields.
- Etiquetas and Lista de verificación (checklist) are also converted to the same collapsible-icon-button pattern.
- Each of the five buttons (Inicio, Fin, Ubicación, Etiquetas, Lista de verificación) has a custom-designed icon (not a generic/stock icon), generated via the Higgsfield/HuggingFace asset-generation tooling.
- Meaningful animation is added wherever it improves the feel of the modal's new interactions (e.g. expand/collapse transitions), using the same tooling where applicable.
- All of the above verified live in a real browser — including opening a card that already has saved dates/location/labels/checklist data, to confirm nothing previously saved is lost or hidden incorrectly — against the live Supabase backend.

## Notes
- 2026-07-21: goal confirmed via a fifth HelmControl New Goal intake ("Start This Goal"). Per this run's explicit scope limit, no implementation was started — handed off to organizer immediately after writing this to break the board-page-scroll fix and the card-modal restructuring (including custom icon generation and animation) into concrete TASKS.md items.
- Confirmed via a quick read of the current code before writing this: `CardDetailModal.tsx` already has working Inicio/Fin (`start_date`/`end_date`), Ubicación (`location_data`, added for Mapa in T096), Etiquetas, and a functioning Lista de verificación / checklist feature (`checklists`/`checklist_items` tables and UI, already in place). None of these are new data or new backend work — this goal is a pure UI/UX restructuring of fields that already exist and already persist correctly, not a request to build the checklist feature from scratch.
- 2026-07-21, same day: T129-T138 fully implemented and verified live — see TASKS.md's consolidated Done entry for the full breakdown. Fanned out to 4 background specialists (board-scroll fix, modal restructure+animations, icon generation, icon integration), the first three running in parallel since their file sets never overlapped. One real regression caught and fixed during the centralized verification pass (not by any individual specialist, by design): `tests/e2e/board-flow.spec.ts`/`permissions.spec.ts` interacted with label/checklist UI that's now collapsed by default, fixed with a shared `openCardField()` test helper. `npm run build`/`lint` clean, `tests/e2e/` 8/8 (some one-off flakiness under 6-worker parallelism, matching this repo's already-documented live-backend-contention pattern, not a regression — confirmed via repeated clean full-suite reruns).
- The prior goal below (rebrand + mobile-responsive redesign) is complete per its own Status line; its history, plus the older Google OAuth blockers (T066/T077/T068/T069/T094b) carried inside it, are unchanged and not re-litigated here.

---

# Previous goal (2026-07-21 — rebrand + mobile-responsive redesign, complete)

**Set:** 2026-07-21 (new goal, confirmed via a fourth HelmControl New Goal intake — supersedes the 2026-07-21 board-scoped-external-API goal below as the actively-narrated goal. That goal's own outstanding items (the pre-existing Google OAuth blockers) are carried forward unchanged as Pending TASKS.md entries, not dropped.)
**Status:** done (2026-07-21) — both "Done when" items implemented and verified live (T114-T128 in TASKS.md's Done section), including a phone-viewport (375×812) `chromium.launch()` pass covering signup through every board view and settings panel with zero horizontal-overflow findings. Only unverifiable piece is Google Calendar event branding (T119 #4), which is blocked on the same pre-existing OAuth consent-screen issue as the prior goal below, not a gap in this goal's own work.

This request has two parts. First, a full rebrand: replace every user-facing and internal reference to "Trello" throughout the app (UI text, labels, docs, page titles, in-app help/API docs written earlier) with "Task Management System" (short form "TAMS"), so nothing in the product still reads as a Trello clone by name. Second, a mobile-responsive fix: phone screenshots shared by the user show the current mobile web view as broken and "chopped up" (elements overlapping/cut off, not laid out cleanly), and the mobile experience needs to be redesigned to look clean, polished, and natural on phone screens rather than just patched.

## Done when
- Every user-facing and internal reference to "Trello" (UI text, labels, page titles, in-app help/API docs) is replaced with "Task Management System" / "TAMS" — nothing in the product still reads as a Trello clone by name.
- The mobile web view is redesigned so no elements overlap, get cut off, or look "chopped up" at phone screen widths — the layout reads as clean and polished, not patched.
- All of the above verified live in a real browser, including at mobile viewport sizes, against the live Supabase backend.

## Notes
- 2026-07-21: goal confirmed via a fourth HelmControl New Goal intake ("Start This Goal"). Per this run's explicit scope limit, no implementation was started — handed off to organizer immediately after writing this to break the rebrand and the mobile-responsive redesign into concrete TASKS.md items. Organizer breakdown complete: rebrand tasks T114-T119 (UI text, page title, Calendar descriptions, ICS metadata, README, end-to-end verification), mobile-responsive tasks T120-T128 (audit, header/nav fix, Kanban layout, Tabla/Cronología/Panel/Mapa responsive redesign, modal/form fixes, end-to-end verification).
- 2026-07-21, same day: T114-T128 fully implemented and verified live — see TASKS.md's consolidated Done entry for the full breakdown (rebrand text/metadata swaps done directly, mobile-responsive work fanned out to 6 parallel `frontend-developer` agents by disjoint file scope, one real scoping gap caught and fixed directly — `ListColumn.tsx`'s list-column width lived outside the agent's given file scope — plus a real touch-only-device bug found and fixed along the way, board deletion on `DashboardPage.tsx` was unreachable on mobile). `npm run build`/`lint` clean, `tests/e2e/` 8/8, live phone-viewport verification with zero overflow findings across 15 checkpoints.
- The prior goal's own outstanding items (the Google OAuth blockers: T066/T077/T068/T069/T094b) are unchanged and not re-litigated here; see that goal's own history immediately below.

---

# Previous goal (2026-07-21 — board-scoped external API integration)

**Set:** 2026-07-21 (new goal, confirmed via a third HelmControl New Goal intake — supersedes the 2026-07-16/07-17 view-switcher goal below, which is complete. Re-confirms the 2026-07-15/07-16 goal's scope almost verbatim and adds exactly one genuinely new requirement not previously in scope: per-board external API integration. The 2026-07-15/07-16 goal's own outstanding items, all user-action-blocked, are carried forward unchanged as Pending TASKS.md entries, not dropped.)
**Status:** in progress — the new external-API requirement is implemented, deployed, and live (T103-T112, user ran the deploy + secret commands 2026-07-21). Everything else in this goal's "Done when" list was already met before this run (see the bullets below) except the pre-existing Google OAuth blockers (T066/T077/T068/T069/T094b), which are unchanged and still need the user — that's the only thing left keeping this goal from being fully done.

Building a private, one-to-one Trello clone for a single organization (not public), backed by Supabase for auth, database, and file storage. Scope: multi-user accounts with roles/permissions on top of Supabase Auth, invites by username or email; card attachments of any file type (images, scripts, zip files) plus links, with image thumbnails shown on cards even when unselected (most recent image as default cover, with a manual "make cover" override like Trello); customizable board backgrounds per member (color wheel or uploaded photo); an in-app-only notification center (invites and other events); card creation timestamps; a visual-only completion checkmark per card that persists to the DB and syncs in realtime to all users without touching any workflow logic; Google Calendar integration combining both an in-app calendar view with a subscribable feed link (no OAuth) and full two-way OAuth sync to each member's personal Google account (registered as an internal/testing-mode Google Cloud OAuth app since membership is limited to this org); and an external API integration feature where each board can have one or more API keys, implemented with both directions since the described protocol needs both — a webhook that pushes out whenever a table changes, and a REST API (POST/GET) external systems can call to pull data or push changes in, with those changes reflected back in the table — plus documentation for both. Already-fixed along the way: the drag-and-drop disappearing-card glitch and the white-rectangle horizontal-scroll overflow bug.

## Done when
- Multi-user accounts and permissions work on top of Supabase auth, with invites by username or email — **already met**, see the 2026-07-15/07-16 goal's Done section below and TASKS.md's Done entries.
- Boards support fully customizable backgrounds (color wheel or uploaded photo), both at board level and individually per-member — **already met** (T080-T092).
- Cards accept attachments of any file type plus links, with image thumbnails shown on the card front (most recent image as default cover, manual "make cover" override) even with no card selected — **already met**.
- An in-app-only notification center surfaces invites and other events (including task completions) — **already met**.
- Cards display a creation timestamp — **already met**.
- Cards have a visual-only completion checkmark that persists to the DB and syncs live to every other member in realtime, without driving any workflow/automation logic — **already met** (T080-T092).
- A calendar view shows cards by date, offers a subscribable ICS feed link (no OAuth), and supports full two-way OAuth sync with each member's Google account (testing-mode OAuth app) — **implemented and code-reviewed, but not fully live-verified**; see Notes and TASKS.md's Pending section (T066, T077, T068, T069, T094b) for the exact remaining user-side blockers.
- Each board can have one or more API keys. An outbound webhook fires whenever a row in that board's data changes. A REST API (POST/GET) lets external systems pull board/card data and push changes in, with pushed changes reflected back in the live table (not a one-way mirror). Both directions are documented. — **fully done and live-verified**: T103-T111 implemented, T112 (deploy + `WEBHOOK_DELIVERY_SECRET`) run by the user 2026-07-21. Beyond the auth-rejection checks, ran one real end-to-end pass against the live project with a generated key: `generate_api_key` → `GET api-board-data` (200, real board/list data) → `POST api-board-mutation create_card` (201) → a follow-up `GET` showing the new card → a cross-board-scope probe correctly 404ing. This is genuinely done, not just deployed-looking.
- The drag-and-drop disappearing-card glitch and the white-rectangle horizontal-scroll overflow bug are fixed — **already met** (a38d58f).
- All of the above verified live in a real browser against the live Supabase backend (and, for the external API, real HTTP requests against a live board).

## Notes
- 2026-07-21: goal re-confirmed via a third HelmControl New Goal intake ("Start This Goal"). Diffed the confirmed summary against goal.md's existing history and TASKS.md's Done/Pending sections: every requirement in the summary already matches implemented-and-verified, implemented-but-blocked-on-the-user, or already-fixed scope from the 2026-07-15/07-16 goal below — **except** the external API integration (board-scoped API keys, outbound webhook on table change, REST pull/push API with changes reflected back in the table, plus documentation), which appears nowhere in prior goal.md or TASKS.md content and is genuinely new.
- The Google Calendar/OAuth blockers are unchanged from before and are not re-litigated here: TASKS.md's T066 (consent screen "Testing" status + test users, needs the user in Google Cloud Console), T077/T068/T069 (live verification, blocked on T066), and T094b (calendar-feed redeploy with `verify_jwt = false`, needs the user to run `supabase functions deploy calendar-feed`) all still apply as-is.
- Per this run's explicit scope limit, no implementation was started. Handed off to organizer immediately after writing this to break the external API/webhook feature into concrete TASKS.md items (key management/storage, RLS-safe key auth for external requests, the outbound webhook delivery mechanism, the REST endpoints themselves, and docs) — the existing Google OAuth Pending items (T066/T077/T068/T069/T094b) are carried forward unchanged, not reworked.
- 2026-07-21, same day: T103-T111 implemented (schema/RPCs, Edge Function code, board "Integraciones" UI panel, README docs) — see TASKS.md's new Done entry for the full breakdown. This goal's external-API "Done when" bullet is now met except for one user-side action: deploying the three new Edge Functions (T112, same recurring `supabase functions deploy` blocker as the Google Calendar functions before it).

---

# Previous goal (2026-07-16/07-17 — view switcher, complete)

**Set:** 2026-07-16 (new goal — supersedes the 2026-07-15/07-16 goal below, whose own outstanding items are carried forward as Pending TASKS.md entries, not dropped)
**Status:** done — all "Done when" items implemented and verified live (see TASKS.md's T095-T102 entry, 2026-07-17), including a same-day Mapa upgrade to real Leaflet. Only loose end: throwaway test data from the verification script (a few boards and a test user, from repeated verification runs) is still live in the database — the delete is hard-blocked by this environment's destructive-action guardrail even with explicit user authorization, so it needs the user to run it themselves (exact command in TASKS.md's T101 entry).

Direct user request, illustrated with a screenshot of real Trello's per-board view switcher (Tablero / Tabla / Calendario / Cronología / Panel / Mapa — in real Trello everything past Tablero is paywall-locked): add the same set of board view types to this clone, all unlocked for every member (no paywall in an internal tool). "Tablero" (Kanban) already exists. The others are new: **Tabla** (a spreadsheet/table view of a board's cards), **Cronología** (a timeline/Gantt-style view laying cards out along their start/end dates), **Panel** (a dashboard/stats view — e.g. cards per list, completion rate, overdue count), and **Mapa** (a map view of cards, which needs a new location field on cards that doesn't exist yet). "Calendario" already exists in this app but today as a separate global nav link (all boards at once) rather than a per-board view-switcher tab like the screenshot shows — reconciling that is part of this goal's scope, not a given.

## Done when
- A board's page offers a view switcher (matching the screenshot's Tablero/Tabla/Calendario/Cronología/Panel/Mapa set) that changes how that same board's cards are displayed without navigating away from the board.
- Tabla: a table/spreadsheet view listing the board's cards as rows (at minimum title, list, labels, dates, complete status) with the same click-through to the existing card detail modal.
- Cronología: a timeline view plotting cards along a date axis using their existing `start_date`/`end_date`.
- Panel: a dashboard view with basic at-a-glance stats for the board (e.g. cards per list, % complete, overdue count).
- Mapa: a map view of cards that have a location set, with a new schema field added to `cards` to hold it.
- Calendario is reconciled into this same per-board view-switcher pattern (or a deliberate, documented decision is made to keep it global instead, with the reasoning written down).
- None of the above are gated behind any kind of paywall/lock — every board member sees and can use every view.
- All of the above verified live in a real browser against the live Supabase backend.

## Notes
- 2026-07-17, later same day: user explicitly asked for Leaflet ("I do want the leaflet installed and being used") and approved the install themselves. `MapView.tsx` rewritten around a real `leaflet` map instance — actual pannable/zoomable OSM tiles, real click-triggering markers, no more iframe-plus-list workaround. See TASKS.md's T101 entry for the rewrite details.
- 2026-07-17: T095-T102 implemented and live-verified in one pass (view switcher, Tabla, Cronología, Panel, Mapa) — see TASKS.md's Done entry for the full writeup. All "Done when" items above are now met. Mapa was built without adding a new dependency (Leaflet install was blocked by this environment's dependency-install guardrail, pending user approval) — an OpenStreetMap iframe embed plus a clickable pin list stood in for it initially, later upgraded to real Leaflet per the note above.
- **Calendario reconciliation (T095, 2026-07-16):** decided to keep Calendario global rather than fold it into the per-board switcher. Reasoning: `CalendarPage` already aggregates every dated card across every board the user belongs to, which is strictly more useful than a board-scoped calendar for a small internal tool, and reworking its fetch to take a `boardId` plus deciding the fate of the existing `/calendar` nav link would be a lot of restructuring for a strictly worse view. The view-switcher (T097) still lists "Calendario" alongside the other five labels for visual parity with the screenshot, but clicking it navigates to the existing `/calendar` route instead of swapping content inline — the one deliberate exception, called out with a code comment where the switcher is defined.
- 2026-07-16: this goal added after fixing an unrelated bug report (the notifications panel rendering broken/squished on BoardPage — see TASKS.md's most recent Done entry). The user then shared two screenshots: their current board, and real Trello's view-switcher sidebar (Tablero unlocked, Tabla/Calendario/Cronología/Panel/Mapa all lock-icon-gated in real Trello's free tier) with "this is everything I want to show in this project." Explicitly scoped to writing this goal + TASKS.md breakdown only this turn — no implementation started yet, per the user's own "that would be it for now."
- Open design questions for whoever scopes/implements this (deliberately not pre-decided here): whether Tabla/Cronología/Panel/Mapa live as tabs inside `BoardPage.tsx` itself or as separate routes like `/calendar` already is; what "location" means for Mapa (a free-text address needing geocoding, vs. plain lat/lng inputs) and what map rendering approach fits this app's no-speculative-dependencies convention so far (e.g. a static embed/iframe vs. a mapping library); and whether Panel needs real chart rendering or can start as plain stat tiles.
- The prior goal below (2026-07-15, re-confirmed 2026-07-16) is not abandoned, just no longer the actively-narrated "current goal" here — its own Done-when items are fully met except for the parts still blocked purely on the user's own actions (Google Cloud Console + two `supabase functions deploy` runs), which remain tracked as TASKS.md Pending items T066/T075b/T077/T067/T068/T069/T093/T094 exactly as before. See that goal's history immediately below for the full record.

---

# Previous goal (2026-07-15, re-confirmed 2026-07-16)

**Set:** 2026-07-15 (re-confirmed and expanded via a second HelmControl New Goal intake on 2026-07-16)
**Status:** in progress (blocked entirely on user-side actions — see Notes)

A Trello-style kanban app for a defined organization (not public), backed by Supabase for auth/database. Multi-user with accounts, permissions, and invites to boards by username or email. Boards support fully customizable backgrounds (color wheel or user-uploaded photo) on both a board level and adjustable per-member. Cards support attachments of any file type (images, scripts, zips, links) with image previews shown directly on the card face — even with no card selected — using the most recently added image as the cover, with an explicit "make cover" option to override. Cards have a visual-only completion checkmark (no backend logic beyond saving the state) that is synced so all members see when someone marks a task complete in real time. Cards show a "created at" timestamp. An in-app-only notification section covers board invites and other events (like task completions). A calendar view shows cards by due date, offers a subscribable feed link for Google Calendar, and also supports full two-way OAuth sync with each member's individual Google account (Google Cloud OAuth app in testing mode, org members added as test users — no public verification needed). Recent bug fixes are also in scope: drag-and-drop cards disappearing mid-drag, and a white-rectangle rendering glitch on far horizontal scroll.

## Done when
- Multi-user accounts and permissions work on top of Supabase auth: board creators can invite others by username or email, and non-owner members are correctly gated from owner-only actions.
- Boards support fully customizable backgrounds via both a color picker and an uploaded photo, at the board level.
- Board backgrounds are also individually adjustable per-member (a member can override the board's background for their own view without changing what other members see).
- Cards accept attachments of any file type plus links; image attachments show an inline thumbnail preview.
- An in-app notification section surfaces board invites and other board events, including task completions (no email delivery).
- Cards display their creation timestamp.
- Card fronts show a cover image without opening the card — defaulting to the most recently added image attachment, with a manual "make cover" override that persists.
- Cards have a visual-only completion checkmark on the card front that toggles and persists without driving any other backend behavior (no filtering/automation tied to it), and that update is synced live to every other member viewing the board (real-time, not just on reload).
- Dragging a card between lists never makes it disappear mid-drag; scrolling the board as far right as it goes never reveals a stray white (or any other non-background-colored) rectangle.
- Cards have a start-date field (optionally a plain deadline with no end time, or a real meeting with an end time) and a calendar view that shows cards laid out by date, with a time range shown for meeting-style cards.
- The calendar view offers a subscribable feed link (ICS) that an external calendar app (e.g. Google Calendar's "From URL" import) can subscribe to for read-only updates.
- Each user can connect their own Google account via OAuth and get full two-way sync between their cards' dates and their Google Calendar: card date changes propagate to Google Calendar as either an all-day event (deadline-style) or a timed event with a real start/end (meeting-style), and changes made on the Google Calendar side propagate back onto the card, correctly distinguishing all-day from timed events.
- The Google OAuth consent screen runs in "testing" publish status with organization members added as test users — no public verification flow required.
- All of the above verified live in a real browser against the live Supabase backend (and a real connected Google account for the calendar sync), not just via unit/type checks.

## Notes
- Origin: confirmed via HelmControl New Goal intake chat on 2026-07-15 ("Start This Goal").
- Per TASKS.md's Done section, everything above except the calendar view/Google sync and the completion checkmark was already implemented and verified live from prior sessions: accounts/permissions/RLS, username-or-email invites, customizable backgrounds, attachments with image thumbnails and card cover images, notifications, created timestamp, and both the drag-disappear and white-rectangle bug fixes.
- All code for the completion checkmark (T053-T055), due date + calendar view (T057-T061), and Google OAuth + two-way sync (T062-T065) is now implemented, security-reviewed (one critical OAuth CSRF/account-linking gap found and fixed — see TASKS.md's T062-T065 entry), audited, and passing `npm run build`/`tsc -b`/the full `tests/e2e/` suite (8/8). The three new migrations were applied live via `supabase db push`.
- **Still open, all needing the user:**
  1. **T063b** — the three new Edge Functions (`google-oauth-exchange`, `google-calendar-push`, `google-calendar-pull`) are written and reviewed but never deployed: this environment hard-blocks `supabase functions deploy` as a production action and requires the user to run it themselves.
  2. **T066** — no real Google Cloud OAuth Client ID/secret exists yet; `.env`/`.env.example` carry placeholders only. README's new "Google Calendar sync" section has the exact console setup steps (redirect URI, scopes, Testing publish status + test users).
  3. **T056/T067/T068/T069** — none of the new features have been verified live in a real browser this session: the shared Playwright MCP browser was held by a different concurrently-running Claude Code session against this same repo for the entire session and never freed up, and no alternative browser-automation path was available (mac-mcp-server's UI tools need an Accessibility permission grant that isn't in place). T068/T069 additionally need T066's real credentials to reach Google's API at all.
- This goal's "Done when" checklist is therefore not yet met: the calendar view and Google sync are built and reviewed but not yet verified live end-to-end.
- 2026-07-15, later in the same session: the user flagged mid-conversation that a bare due date isn't enough to represent a real meeting (needs a start *and* end time, not just one date) — confirmed via AskUserQuestion, user chose full start+end time support over the simpler "just add a time to the existing due date" option. This is a schema/UI/sync rename+extension (`due_date` -> `start_date` + new `end_date`), implemented and code-verified as T070-T076 in TASKS.md (migration applied live, `npm run build` clean, `tests/e2e/` 8/8 passing), superseding T057-T059/T064-T065's single-date implementation without invalidating the calendar view or OAuth plumbing built alongside it.
- Deployment/credentials update: the user deployed the three Edge Functions themselves (this environment blocks `supabase functions deploy` from me directly) and supplied a real Google OAuth Client ID/secret, which I set live via `supabase secrets set` (not blocked) and mirrored into `.env`'s `VITE_GOOGLE_CLIENT_ID`. Redirect URI (`http://localhost:5173/google-callback`) is configured on the Google side; the consent screen's "Testing" publish status + test users (T066) is not yet confirmed done. The `google-calendar-push`/`google-calendar-pull` functions' code changed again for start/end-time support (T075/T076) after that initial deploy, so they need a second redeploy (T075b) before live verification (T077) can proceed.
- 2026-07-16: goal re-confirmed via a second HelmControl New Goal intake ("Start This Goal"), rewording the goal statement/Done-when list above to that intake's exact confirmed summary. Diffing that summary against TASKS.md's Done section surfaced three genuinely new requirements not previously in scope, none yet implemented:
  1. **Per-member adjustable board backgrounds** — today (T034/T037) board background is a single board-level setting, owner-only. The new requirement adds a per-member override (each member can set their own view of a board's background without affecting others).
  2. **Real-time sync of the completion checkmark** — T055 persists the checkmark via a normal update but wires no live push; other members only see a change on reload/refetch. The new requirement is genuinely real-time (Supabase Realtime or equivalent), plus a notification event for task completions specifically (T041's existing triggers only cover `board_invite`/`member_removed`, not card completion).
  3. **Subscribable ICS feed link for the calendar** — today only the OAuth-based two-way Google sync exists (T057-T076); a separate, simpler subscribable feed URL (e.g. for Google Calendar's "From URL" import) has not been built.
  Everything else in the re-confirmed summary matches already-built-or-in-flight scope covered by the existing Pending/Done items above. Handed off to organizer immediately after this note to break these three items into TASKS.md entries — no implementation done in this handoff run.
- 2026-07-16, later same day: all three of the above (T080-T092 in TASKS.md) implemented and live-verified in one pass. Per-member board backgrounds and real-time completion sync + notifications are both **fully done and verified live** (two-browser-context script, screenshots, `tests/e2e/` 8/8) — those two "Done when" bullets are now genuinely met. The ICS feed is code-complete and its RPC/schema side is live, but the Edge Function itself (`calendar-feed`) is blocked on a `supabase functions deploy` the environment won't let me run directly (same recurring blocker as T063b/T075b) — see TASKS.md's new T094. Found and fixed two real bugs along the way, neither speculative: (1) a Realtime/optimistic-insert race in `handleAddCard` that could duplicate a card in the DOM, caught by `tests/e2e/board-flow.spec.ts` going flaky; (2) the `calendar_feed_token` column's own task spec assumed no RLS/grant work was needed, which was wrong — `profiles`' existing "any authenticated user" SELECT policy plus a table-wide grant would have let anyone read anyone else's feed token; fixed with a column-privilege revoke+re-grant, verified live via `information_schema`.
- This goal's "Done when" checklist is now down to one real blocker category: Google OAuth (T066 consent screen confirmation, T075b redeploy, T077/T068/T069 live verification) plus T094's calendar-feed deploy — all of which need the user to run a command or confirm a console setting themselves.
- 2026-07-17: user ran `supabase functions deploy` themselves, deploying all four Edge Functions (T075b done). Live-verifying the newly-deployed `calendar-feed` (T093) immediately found it non-functional: every request 401s at Supabase's platform gateway (`UNAUTHORIZED_NO_AUTH_HEADER`) before the function's own token-validation code runs, because nothing had ever set `verify_jwt = false` for it — it was never live before today, so this was never testable until now. `calendar-feed` was always meant to be public (external calendar apps can't carry a Supabase session), so this is a real bug, not a design change. Fixed in `supabase/config.toml` (new `[functions.calendar-feed] verify_jwt = false` section, the other three functions correctly keep default JWT verification) — see TASKS.md's new **T094b**, needs one more `supabase functions deploy calendar-feed` from the user before T093 can actually pass. T066 (consent screen) remains the only other blocker, now for T068/T069/T077 specifically.
